---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pod-api-ingress
spec:
  ingressClassName: nginx-example
  rules:
  - http:
      paths:
      - path: "/teste"
        pathType: Prefix
        backend:
          service:
            name: pod-api-svc
            port:
              number: 8085
